{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
    <h3>Défendre</h3>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div>
        <input class="waves-effect waves-light btn-large" type="submit" value="Défendre" />
    </div>
    {{ form_end(form) }}
    </div>
{% endblock %}