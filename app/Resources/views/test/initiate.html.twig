{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
    <h3>Faire un jet</h3>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    </div>
{% endblock %}