<ul id="slide-out" class="side-nav">
    <li>
        <div class="user-view">
            <div class="background">
                <img src="{{ asset('assets/images/chevalier.jpg') }}">
            </div>
            {% if app.user %}
                <a href="{{ path('fos_user_security_logout') }}"
                   class="btn-floating right btn-large waves-effect waves-light red"><i class="large material-icons">power_settings_new</i></a>
            {% endif %}
            <a href="#!user"><img class="circle" src="{{ asset('assets/images/batclip.jpg') }}"></a>
            {% if app.user %}
                <p class="white-text flow-text">{{ app.user.username }}</p>
            {% endif %}
        </div>
    </li>
    {% if is_granted('ROLE_ADMIN') == false %}
        {% if app.user %}
            <li><a href="#!"><i class="material-icons">account_box</i>Voir mon personnage</a></li>
        {% else %}
            <li><a href="{{ path('fos_user_security_login') }}"><i class="material-icons">cloud</i>Se connecter</a></li>
        {% endif %}
    {% endif %}
    <li>
        <div class="divider"></div>
    </li>
    {% if app.user %}
        <li><a class="subheader">Partie</a></li>
        <li><a class="waves-effect" href="{{ path('dashboard') }}">Actions</a></li>
        <li><a class="waves-effect" href="{{ path('tests') }}">Historique</a></li>
    {% endif %}
</ul>
<div class="fixed-action-btn">
    <a href="#" data-activates="slide-out" class="button-collapse btn-floating waves-effect waves-light btn-large"><i
                class="material-icons">menu</i></a>
</div>